<article class="item">
	<div>{{model.title}}</div>
	{{#if model.image }}
		<img src="{{model.image}}">
	{{/if }}
	{{#if model.description }}
		<div>{{model.description}}</div>
	{{/if }}
	{{#if model.links }}
		<div class="links" data-hook="links"></div>
	{{/if }}
</article>
