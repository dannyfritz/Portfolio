---
  layout: layout
  title: About Me
  tags: nav
---

<ul>
{%- for section in bio.sections -%}
<li>
  <h3>{{section.title}}</h3>
  <ol>
    {%- for link in section.links -%}
    <li>
      <a href="{{link.url}}">{{link.text}}</a>
    </li>
    {%- endfor -%}
  </ol>
</li>
{%- endfor -%}
</ul>
