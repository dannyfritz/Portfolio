---
  layout: layout
  title: About Me
  tags: nav
---

{%- for section in bio.sections -%}
<h3>{{section.title}}</h3>
<ol>
  {%- for link in section.links -%}
  <li>
    <a href="{{link.url}}">{{link.text}}</a>
  </li>
  {%- endfor -%}
</ol>
{%- endfor -%}
